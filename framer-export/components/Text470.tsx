import React from 'react';

interface Text470Props {
  label?: string;
}
export default function Text470({ label }: Text470Props) {
  return (
    <text>Pleasure to work with, 5 stars, can't recommend enough!</text>
  );
}
